name: My First Workflow

on: push

jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: Welcome Message
              run:  echo "My First Githbub Actions Job!"

            - name: List Files
              run:  ls

            - name: Read File
              run:  cat README.md